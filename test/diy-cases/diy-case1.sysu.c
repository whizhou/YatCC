int sum(int count, ...);
int main(){
    // This is a text
    /* This is a test */
    int x = 0;
    x <<= 1 << 2 < 3;
    x >>= 2 >> 1 > 1;
    x + x++;
    x - x--;
    x + ++x;
    x - --x;
    int inty = 1 << 2 + (x <<= 2) <= 4;
    float z = 0.1;
    float a0x = 0.0 <= 1e-10 + 12.0e+1 - 10.00e2 * 1e+2;

    unsigned long long ull_x = 1llu;
    unsigned long ul_y = 2ul;

    return 0;
}